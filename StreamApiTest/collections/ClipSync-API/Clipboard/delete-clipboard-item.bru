meta {
    name: Delete Clipboard Item
    type: http
    seq: 3
}

delete {
    url: {{baseUrl}}/api/clipboard/{{itemId}}
}

headers {
    Authorization: Bearer {{token}}
    x-csrf-token: {{csrfToken}}
}
