meta {
    name: Restore Version
    type: http
    seq: 5
}

post {
    url: {{baseUrl}}/api/versions/{{versionId}}/restore
}

headers {
    Authorization: Bearer {{token}}
    x-csrf-token: {{csrfToken}}
}
