meta {
    name: Login
    type: http
    seq: 3
}

post {
    url: {{baseUrl}}/api/auth/login
    body: json
}

headers {
    Content-Type: application/json
}

body:json {
    {
        "phone": "{{phone}}",
        "password": "{{password}}"
    }
}
