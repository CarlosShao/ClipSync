# 研究计划：ClipSync 桌面端功能客观审查（面向移动端决策）

日期：2026-08-28
分支：feature/ai-module-refactor

## 用户核心诉求
不以文档为依据，从**实际代码实现 + 产品价值**出发审查桌面端，判断是否可转向移动端开发。

## 五个审查维度
1. 核心功能完整性：剪贴板捕获/历史/搜索/同步等是否覆盖主要场景，有无明显缺口
2. 功能深度与细节：是"能用"还是"好用"；异常处理、边界情况
3. 冗余与价值判断：低价值/冗余功能识别；真正值得补的能力
4. 与移动端的衔接：同步机制、操作逻辑、跨端一致性风险
5. 结论建议：能否转向移动端；未达标时优先补什么

## 查询类型
Depth-first：对同一对象（桌面端）做多视角深查。分解为 4 个可并行的代码审查子任务。

## 信息检索策略
- 主力：代码探索（code-explorer subagent × 4），覆盖 src/desktop/src/**、src/desktop/src-tauri/**、src/server/src/routes/**（API 面）
- 辅助：主代理自查 git log 近期提交、README 中产品定位（仅定位参考，不作功能依据）
- 微信文章搜索：本任务为纯内部代码审查，无外部信息需求，不适用 wechat-article-search

## 子代理分配
- Agent A【核心功能完整性】：盘点全部功能模块实现状态（剪贴板捕获/去重/历史/搜索/过滤/收藏/集合/标签/多设备同步/AI 模块/模板/快速粘贴/文档抽屉/设置/隐私保护/粘贴预测/导入导出），判断完成度（完整/半成品/占位），找主要场景缺口
- Agent B【深度与细节】：异常处理与边界（断网、后端不可达、大文件、图片失败、并发冲突、token 过期、Rust 捕获失败）、加载态/失败重试/降级、数据一致性（乐观更新回滚）
- Agent C【冗余与价值】：识别低使用率/冗余/定位不符功能（7 主题、文档抽屉、满意度调查、CoachMarks、模板系统等），并评估缺失能力（全局搜索增强、置顶、多格式预览、回收站等）的价值
- Agent D【移动端衔接】：同步机制（WebSocket 事件、轮询、冲突处理、增量拉取）、桌面强绑定点（全局快捷键、右键菜单、hover、多窗口、托盘）、API 对移动端友好的部分与阻碍

## 综合输出
research_report_desktop_review.md，含五维度结论 + 明确的"能否转移动端"判断与优先级建议。
