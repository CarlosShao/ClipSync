# ClipSync 缺失 / 不完整功能清单（按端分类）

> 更新时间：2026-07-21
> 范围：桌面端（Windows Tauri）为主开发端；移动端、Web 后台、后端能力、架构债务单列，均非桌面端直接范围。
> 状态图例：**✅ 已实现** | **🔶 部分实现/不完整** | **❌ 未实现** | **❓ 待核查** | **🚫 搁置** | **🚮 已决定不做**

---

## 〇、已实现功能（代码实测 + 用户确认）

| 功能 | 说明 |
|------|------|
| Markdown 预览 / 代码高亮 | 已落地 |
| 批量删除 | 已落地 |
| 设备配对二维码 | 仅配对用 |
| Windows 剪贴板监听 | 事件驱动 + 序列号扫描 |
| 文档预览 Drawer | Markdown/Word/Excel/PDF |
| 模板库 | 含持久化 |
| 分享链接 | 前后端已打通 |
| 条目级密码保护 | `useItemPassword.ts` + `useProtection.ts` + 后端 `/api/protection/*` |
| 高级搜索过滤器 | 日期 / 设备 / 类型 / 标签 |
| 收藏夹全部功能 | 侧边栏入口、独立视图、排序、分组标签、批量、实时同步 |
| 图片预览 object URL 释放 | 已落地 |
| 设备管理（桌面 UI） | `settings/DevicesView.vue` 已存在 |
| 桌面端内存监控 | `utils/perfMonitor.ts` 已存在 |
| 大文件分片上传 | `utils/chunkedUpload.ts` + 后端 `chunked-upload.js` 已存在 |

### 已决定不做

| 功能 | 原因 |
|------|------|
| 剪贴板内容 → 二维码 | 用户确认没必要 |
| 普通剪贴板列表拖拽重排 | 用户确认无意义 |

---

## 一、桌面端（Windows Tauri）— 主开发范围

### 1.1 高级剪贴板功能

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 归档 archive | ❌ | P1 | 仅图标，无功能 |
| 表格预览 | ❓ | P1 | 指 TSV/CSV 文本渲染成表格，非 Excel 预览（Excel 已支持） |
| HTML 预览 | ❓ | P1 | 指 HTML 片段安全渲染（沙箱 / DOMPurify），当前无组件 |
| 自动过期清理（用户侧） | 🔶 | P1 | 后端 `cleanup.js` 有 Job，前端无设置入口 |

### 1.2 搜索与过滤

| 功能 | 状态 | 优先级 |
|------|------|--------|
| 保存的搜索 | ❌ | P1 |
| 搜索历史 | ❌ | P2 |
| 正则表达式搜索 | ❌ | P1 |

### 1.3 收藏夹

- ✅ 全部完成

### 1.4 AI / 智能功能

| 功能 | 状态 | 优先级 |
|------|------|--------|
| 图片 OCR | ❌ | P1 |
| 智能建议 / 预测粘贴 | ❌ | P2 |
| 语言检测 | ❌ | P2 |
| 真正 ML 模型（本地轻量 V2） | ❌ | P2/V2 |
| AI 自动分类 / 标签 | ❌ | P2 |

### 1.5 认证、账户与安全

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 2FA / OTP 登录 | ❌ | P1 | |
| OAuth 登录 | ❌ | P1 | |
| 生物识别登录 | ❌ | P1 | |
| 登录历史查看 | ❌ | P1 | 桌面入口缺失 |
| JWT 黑名单 / 会话撤销（注销立即失效） | 🔶 | P0 | 后端已实现，需复测 |
| 密钥管理 / 每设备独立密钥 | ❌ | P1 | 现单一 `ENCRYPTION_KEY` |
| 审计日志查看器 | ❌ | P0/P1 | 后端 `audit.js` 已存在，前端无入口 |

### 1.6 性能与运维

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 列表分页 / 无限滚动 | 🔶 | P1 | 后端支持 page/limit，前端无无限滚动；虚拟滚动已舍弃 |
| 离线队列跨用户清理 | ❓ | P1 | logout 是否清理 content-cache / offline-queue 待核查 |
| 大文件分片上传（multer 改 diskStorage） | 🔶 | P1 | 前后端已有分片逻辑；后端 `media.js` 仍用 memoryStorage |

---

## 二、移动端（Android / iOS / macOS）— 独立端

| 功能 | 状态 | 说明 |
|------|------|------|
| macOS 桌面端 | 🚫 | 阶段六搁置后延 |
| iOS 移动端 | 🚫 | 阶段七搁置后延 |
| Android / Flutter 移动端 | ❓ | 主体已实现，剩余优化见移动端指南 |

---

## 三、Web 后台管理 — 规划中，不急

> 用户确认：应用将来需要独立后台管理 Web 应用，当前不急。

| 后台能力 | 状态 |
|----------|------|
| 用户 / 账户管理后台 | ❌ |
| 设备管理后台 | ❌ |
| 登录历史查看 | ❌ |
| 审计日志查看器 | ❌ |
| 订阅 / 支付管理后台 | 🔶 |
| 通知运营后台 | ❌ |
| 企业能力后台（团队 / RBAC / SSO） | ❌ |
| API Key / Webhook 管理后台 | ❌ |

---

## 四、后端 / 服务端能力（跨端共享）

### 4.1 企业级功能

| 功能 | 状态 | 优先级 |
|------|------|--------|
| 团队 / 工作区 | ❌ | P3 |
| 共享剪贴板 | ❌ | P3 |
| RBAC | ❌ | P3 |
| SSO / SAML | ❌ | 外部依赖 |
| API Key 管理 | ❌ | P1/P2 |
| Webhook 支持 | ❌ | P2 |

### 4.2 支付与商业化

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 真实支付渠道（微信/支付宝/Stripe） | ❌ | 外部依赖 | 需商户号 |
| 桌面端完整支付 UI | 🔶 | P1 | 移动端已做，桌面端待补齐 |
| 订阅管理完整版 | 🔶 | P1 | 部分完成 |

### 4.3 通知与运营

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 邮件通知 | ❌ | P2 | 需 SMTP / 服务商 |
| 应用内通知中心完整版 | 🔶 | P1 | 后端 routes 存在，推送点是否落库待验证 |
| 用户活动分析仪表板 | ❌ | P2 | |
| 功能发现 / 新功能介绍 | ❌ | P2 | |
| 推荐奖励系统 | ❌ | P2 | |

### 4.4 安全 / 端到端

| 功能 | 状态 | 优先级 | 说明 |
|------|------|--------|------|
| 端到端加密真正集成（上传密文） | ❌ | P1 | 主同步仍传明文 |
| JWT 黑名单 / 会话撤销 | 🔶 | P0 | 见 1.5 |
| 密钥管理 / 每设备独立密钥 | ❌ | P1 | 见 1.5 |

---

## 五、跨端架构债务

| 问题 | 状态 | 优先级 |
|------|------|--------|
| 端到端加密未真正集成（上传明文） | ❌ | P1 |
| 文件存储本地磁盘，多实例无法共享 | ❌ | P0 |
| PostgreSQL / Redis 单实例 SPOF | ❌ | P0 |
| 无 Sentry / APM / 日志聚合 | ❌ | P0 |
| 无审计日志 | 🔶 | P0 | 后端 `audit.js` + 迁移已存在，集成不全 |
| 无对象存储 / CDN | ❌ | P0 |
