# Grafana Dashboard 自动加载配置
apiVersion: 1

providers:
  - name: 'ClipSync Dashboards'
    orgId: 1
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true
