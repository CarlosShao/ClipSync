## 2026-06-26 工作记录

### ✅ 已完成

1. **解决Docker容器CPU/内存爆炸问题**
   - 根因：nodemon配置导致无限重启循环（每次重启都执行 npm install -g nodemon）
   - 解决：去掉nodemon，改为直接运行 `node src/index.js`
   - 验证：CPU 0%，内存 ~55MB

2. **全量Docker数据迁移（Bind Mount）**
   - 迁移 ClipSync: `postgres_data` → `./data/postgres/`, `redis_data` → `./data/redis/`
   - 迁移 word-helper-mobile: `postgres-data` → `freework-workspace/.../data/postgres/`
   - 迁移 v2/refactored 数据到独立目录
   - 删除所有旧Docker managed volumes
   - **效果：WSL/Docker崩溃不会导致数据丢失**

3. **修复CSRF测试（15个测试，全部通过）**
   - `generateCsrfToken` 和 `validateCsrfToken` 是 async 函数，测试中漏加 await
   - 修复 `csrf.js` 的 token 日志错误（token 可能不是字符串）

4. **修复 docker-compose.dev.yml**
   - 添加 `./tests` 目录挂载（测试文件修改能同步到容器）
   - 去掉 nodemon

5. **修复 MaxListenersExceededWarning**
   - `ws/server.js` 添加 `wss.setMaxListeners(100)` 和 `server.setMaxListeners(100)`

### ⏳ 进行中

- 完整测试套件运行中（fork模式，避免worker僵尸问题）

### 📝 下一步

1. 查看测试结果，修复失败的测试
2. 测试 Flutter Web 应用
3. 更新长期记忆文件
