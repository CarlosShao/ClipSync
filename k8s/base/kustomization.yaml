apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clipsync
commonLabels:
  app.kubernetes.io/managed-by: kustomize

resources:
  - namespace-config-pvc.yaml
  - postgres.yaml
  - redis.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - api.yaml
  - ingress.yaml
  - ingress-networkpolicy.yaml

images:
  - name: clipsync/server
    newTag: latest
